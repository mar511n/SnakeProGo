# TODO list
- implement statistics
- add ghost behavior

- make GUI system
	- slider struct (onValueChange callback (when slider is moved))
	- dropdown struct (onSelected callback (when item is selected), contains button structs)
- make Controller menu
	- make accessible controller/key rebinding

- add reload button to reload configs

- add player bots with neat AI

- add audio to replay (hard)
- make mobile controller working (issue with connectivity...)
