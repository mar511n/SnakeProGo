package main

var (
	// BaseSystemPath is the absolute path for the game data
	// This should be set via command line argument, defaulting to $HOME/snakeprogo
	BaseSystemPath = ""

	// ConfigDir is the relative path for configuration files
	ConfigDir = "config"

	// AssetsDir is the relative path for game assets
	AssetsDir = "res"
)
